import { WorkoutTemplate } from '../lib/types';

// Sin plantillas ni ejercicios predefinidos (el usuario crea su propio contenido personalizado)
export const DEFAULT_TEMPLATES: WorkoutTemplate[] = [];
